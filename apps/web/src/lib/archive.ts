/**
 * Re-exported from `@hermit-org/acp-hooks` so web and mobile share the same
 * archive duration parsing and auto-archive selection logic.
 */
export {
  parseDuration,
  selectAutoArchiveIds,
  sessionUpdatedAtMs,
} from "@hermit-org/acp-hooks";
